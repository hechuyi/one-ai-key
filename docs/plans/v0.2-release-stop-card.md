# v0.2 Release Stop Card

plan_id: `operator-confidence-release-plan`

release_or_scope_name: `v0.2 operator-confidence release closure`

closed_capability: `Operator Confidence Release`

implemented_scope: `release artifact smoke; local configuration and model visibility checks; operator command safety reports; bounded failure-evidence contracts; config diagnostic contracts; pre-output stability boundary characterization; stable README, operations, and release-build docs`

deferred_scope: `production deployment smoke; main route/app boundary extraction; v0.3 Conservative Pre-Output Stability v1 implementation`

parked_or_rejected_items: `request-path behavior changes in v0.2; new YAML fields; new management endpoints; new CLI commands; live upstream catalog aggregation; background health scanning; persistent failure ledger; UI, billing, multi-tenancy, and broad protocol conversion`

operator_contract_evidence: `pass: release-smoke exercised extracted artifact, generated local config, authenticated /v1/models, one model-bearing request, doctor, client-tokens, models, route, keys, failures, reload, reload dry-run, invalid client token, and management URL misuse rejection`

test_evidence: `pass: scripts/local-ci.sh passed with 1040 unit tests, 10 local_release_contract tests, and 3 pre_output_stability_boundary_contract tests; local_release_contract rerun passed 10/10; config_diagnostics passed 12/12; pre_output_stability_boundary_contract passed 3/3`

non_empty_filtered_test_evidence: `pass: local_release_contract --list returned 10 tests; config_diagnostics --list returned 12 tests; pre_output_stability_boundary_contract --list returned 3 tests; cli_report --list returned 9 tests; cli_effects --list returned 21 tests; failures --list returned 22 tests; targeted stability filters each returned at least one test before execution`

local_ci_result: `pass: CARGO_TARGET_DIR outside the repository, scripts/local-ci.sh exit 0`

release_artifact_result: `pass: scripts/build-release-x86_64-linux-docker.sh produced dist/one-ai-key-0.1.8-x86_64-unknown-linux-gnu.tar.gz and matching .sha256; sidecar contains archive basename only; checksum verifies; tarball contains only one-ai-key`

release_smoke_result: `pass: scripts/release-smoke.sh exit 0, release smoke passed for one-ai-key 0.1.8`

artifact_sha: `be89053a4c54d62352ce2c60cdd62b4928e0d64611bf184b1b3f97c24447657f`

published_asset_verification: `pass: GitHub Release v0.1.8 uploaded assets were downloaded into a tempdir; checksum verified from the uploaded .sha256 sidecar; tag, release title, asset filename, checksum filename, and artifact sha match one-ai-key 0.1.8; release is not draft or prerelease`

redaction_and_denylist_result: `pass: executable staged-path denylist exited 0; git diff --check passed; git diff --cached --name-only contains no dist/, target/, config/, data/, db/, logs/, keys/, key-pool-router/, AGENTS.md, SQLite, logs, key files, token files, private scripts, local deployment state, real deployment domains, raw keys, production traces, local absolute workspace paths, or support-thread residue. Existing redaction test fixtures containing synthetic token-like strings remain test data, not release residue`

anti_platform_gate_result: `pass: staged diff adds no client-facing protocol expansion, live catalog aggregation, billing or usage analytics, UI or multi-tenancy concepts, background probing, adaptive routing, or automatic discover/apply/reload/scope behavior`

support_residue_scan_result: `pass: README.md, docs/operations.md, docs/release-build.md, release smoke, and plan docs use placeholders and stable product rules rather than private deployment names, temporary keys, production gateway domains, chat history, or one-off workaround transcripts`

deployment_boundary_result: `not_run_by_design: no production server was touched; deployment pin and production smoke remain operator-run workflows documented in docs/operations.md`

no_prod_touch: `pass: no SSH, systemctl, token rotation, production gateway traffic, server build, or deployment host edit was performed in this plan`

route_boundary_extraction: `deferred`

known_blockers: `none for v0.2_local_release_ready`

v0.3_blockers: `client-visible 503/no_route_candidate remains more frequent through the gateway than direct upstream use in observed agent traffic; v0.3 must distinguish local route admission failure from selected-target pre-output transient failure and implement the bounded one-extra-attempt contract without background health checks or persistent ledgers`

next_version_candidates: `Conservative Pre-Output Stability v1; post-release main route/app boundary extraction; config resolver/compiler boundary extraction; failures projection/classification split`
