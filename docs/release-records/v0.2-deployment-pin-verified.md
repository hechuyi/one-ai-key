# v0.2 Deployment Pin Verified Record

Stop node: `v0.2_deployment_pin_verified`

Cargo package version: `0.2.1`

Git tag: `v0.2.1`

Pinned asset:

- `one-ai-key-0.2.1-x86_64-unknown-linux-gnu.tar.gz`

Archive SHA256:

`8411d6ccbaaeb8bf14f1283a059197fcbe9cd9c3364d91b3f1bf2dd980df5ab9`

Verification summary:

- NixOS release pin consumed the GitHub release artifact by URL and hash.
- service unit used the pinned release package: `pass`
- persistent config and state directories remained outside the release package.
- local health and readiness smoke: `pass`
- local authenticated model catalog smoke: `pass`
- management serving health smoke: `pass`
- public `/v1/models` smoke: `pass`
- public `/v1/responses` smoke: `pass`
- operator key helper status command: `pass`

Boundary:

No server-side source build was performed. The deployed package came from the
published release artifact. This record intentionally omits hostnames, public
service domains, private paths, token values, raw responses, and command logs.
