# v0.2 Local Release Ready Record

Stop node: `v0.2_local_release_ready`

Cargo package version: `0.2.1`

Scope: M1-M4 operator confidence baseline.

This record closes the source-tree release-ready stop node only. It records that
the local release gate is expected to pass for the operator-confidence package:
compiled local model visibility, redacted operator reports, explicit key and
reload maintenance workflows, bounded recent failure evidence, runtime
resilience admission diagnostics, release artifact smoke, and the documented
deployment boundary.

Verification summary:

- local CI: `pass`
- release artifact build: `pass`
- artifact shape check: `pass`
- release smoke: `pass`
- staged denylist: `pass`
- public docs: `pass`
- GitHub release publication: `not_run_by_design`
- deployment pin smoke: `not_run_by_design`

Implemented capability:

- one release artifact can be built through the local Docker/Nix x86_64 path;
- the extracted artifact can run the local mock release smoke without a source
  checkout binary;
- `check-config`, authenticated `/v1/models`, `models explain`, and
  `route explain` agree on a representative public model and client-token
  reference;
- key maintenance, model publication, reload, response-filter event, and recent
  failure reports use redacted operator envelopes;
- local `no_route_candidate`, provider soft-cooling last resort, and bounded
  recent failure evidence are visible through management and CLI reports.

Explicit boundaries:

- GitHub release publication is a later stop node, not part of this local
  release-ready record.
- Deployment pin verification is a later operator-run stop node, not implied by
  local source or artifact checks.
- Production smoke remains operator-run and is not a CI or release-smoke
  dependency.
- No request-path retry, routing, endpoint conversion, model exposure, or credential lifecycle behavior changed for this stop node.

Deferred scope:

- hosted multi-tenancy, billing, dashboards, usage ledgers, live model catalog
  aggregation, background health probes, automatic model exposure, endpoint
  conversion, and broad protocol emulation remain outside this release.
- Route/app boundary extraction and config resolver/compiler extraction remain
  architecture debt for separate plans.
